# NOVA OS — نظام تشغيل بالنيّة

> نظام تشغيل عام كامل يعمل داخل المتصفح، لا يقود الذكاء الاصطناعي فيه تطبيقًا
> واحدًا بل يقود **النظام نفسه** — بنفس الصلاحيات التي يملكها المستخدم، لا أكثر.

الدخول: `/os` — **بلا تسجيل دخول**. نوفا نظام قائم بذاته: حالته في متصفحك
ولا يقرأ شيئًا من الخادم. الشيء الوحيد الذي يحتاج حسابًا هو طبقة العصب
(تصرف مفتاح Claude)، وبدونها يعمل النظام كاملًا بطبقة الانعكاس المحلية.

نوفا **غير مرتبطة بأي منتج**: لا نداء في جدولها يخصّ تطبيقًا بعينه، وثمّة فحص
آليّ يمنع تسرّب مثل ذلك مستقبلًا.

---

## الفكرة المعمارية

في الأنظمة المعتادة، الذكاء الاصطناعي ميزة داخل تطبيق: مربّع دردشة في زاوية.
في نوفا، الأمر معكوس — **الذكاء هو المُخطِّط**، والنظام هو ما يُنفّذ.

يقوم كل شيء على قيد واحد:

> **لا شيء في النظام يستطيع تغيير الحالة إلا عبر نداء نظام معرّف ومُتحقَّق منه.**

هذا القيد ليس تجميلًا، وهو ما يجعل كل ما يلي مجانيًا بدل أن يكون مشروعًا منفصلًا:

| النتيجة | لماذا هي مجانية |
| --- | --- |
| **تسجيل كامل** | كل تغيير يمرّ من نقطة واحدة، فيُسجّل هناك — والمُسجَّل هو نيّة المستخدم لا فروعها. |
| **إرجاع زمني حقيقي** | النواة دالة نقية، فإعادة تشغيل السجل تعيد بناء الحالة. |
| **قيادة بالذكاء** | نفس الجدول الذي تنفّذه النواة هو الجدول الذي يتعلّمه المُخطِّط. |
| **حصر صلاحيات الذكاء** | ما لا يوجد في الجدول لا يمكن أن يحدث، ولو طلبه المُخطِّط. |
| **أتمتة معلنة** | القاعدة = حدث + قائمة نداءات. لا كود. |
| **صلاحيات حقيقية** | ما دامت كل قدرة مسمّاة، صار بالإمكان أن تكون *مملوكة*. |

---

## الطبقات

```
النيّة (عربية حرة، صوت المستخدم)
        │
        ▼
┌───────────────────────────────┐
│  Cortex — طبقة العصب          │  Claude، على الخادم فقط
│  src/lib/nova/cortex.ts       │  نيّة → خطة نداءات (خرج مهيكل)
└───────────────────────────────┘
        │ تفشل أو يغيب المفتاح → تراجع تلقائي
        ▼
┌───────────────────────────────┐
│  Reflex — طبقة الانعكاس        │  مُخطِّط محلي حتمي، عربي/إنجليزي
│  src/lib/nova/reflex.ts       │  بلا شبكة وبلا كلفة
└───────────────────────────────┘
        │
        ▼
┌───────────────────────────────┐
│  حاجز التحقق                   │  مخططات zod لكل نداء
│  src/lib/nova/syscalls.ts     │  النداء غير الصالح يُرفض قبل الحالة
└───────────────────────────────┘
        │
        ▼
┌───────────────────────────────┐
│  النواة — دالة نقية            │  (حالة، نداء) → حالة + آثار + سجل
│  src/lib/nova/kernel.ts       │
└───────────────────────────────┘
        │
        ▼
   الحالة · السجل · الإرجاع الزمني
```

**نقطة القرار المهمة:** طبقة الانعكاس ليست «وضع تدهور». هي شرط تصميمي:
نظام تشغيل لا يجوز أن يتوقف عن العمل لأن مفتاح API غائب أو الشبكة سقطت.
الذكاء السحابي يوسّع نوفا ولا يشترطها.

---

## نداءات النظام

الجدول في `src/lib/nova/syscalls.ts` هو المصدر الوحيد للحقيقة: منه تُبنى مخططات
التحقق، ومنه يُبنى **آليًا** الدليل الذي يقرأه Claude (`syscallManual()`) — فلا يمكن
أن يتخلّف وصف القدرات عن الكود.

```
win.open · win.close · win.focus · win.minimize · win.maximize · win.move · win.arrange
fs.write · fs.delete · fs.search
theme.set · notify · say · clipboard.set · power
agent.spawn · agent.kill · app.compose
automation.create · automation.toggle · journal.rewind
```

إضافة قدرة جديدة للنظام = سطر في الجدول + فرع في النواة. لا شيء آخر: الشريط،
والصدفة، والأتمتة، وطبقة الذكاء كلها تكتسبها تلقائيًا.

---

## توليد التطبيقات دون تشغيل كود مولّد

«اصنع لي تطبيقًا لمتابعة شرب الماء» يولّد تطبيقًا حقيقيًا يُثبَّت في الشريط السفلي.
لكن نوفا **لا تُشغّل كودًا مولّدًا أبدًا** — لا `eval` ولا `new Function`.

ما يولّده الذكاء هو *وصف* واجهة بلغة معلنة محدودة (`src/lib/nova/spec.ts`):
عُقد مسموحة (`stack`, `card`, `metric`, `button`, `input`, `list`, `bars`, …)،
وحركات مسموحة (`set`, `inc`, `push`, `clear`, أو نداء نظام). المواصفة تُفحَص
بمخطط، ثم يرسمها مفسّر آمن (`SpecRenderer.tsx`).

أثر ذلك أمنيًا مباشر: **أسوأ ما ينتج عن توليد فاشل واجهة ناقصة، لا ثغرة.**
وأزرار التطبيق المولّد لا تملك إلا نداءات النظام التي يملكها المستخدم أصلًا.

---

## الصلاحيات: التطبيق المولّد ليس النظام

جدول نداءات النظام جعل كل قدرة **مسمّاة**؛ وطبقة الصلاحيات تجعلها **مملوكة**.

الفرق الذي يقوم عليه النظام:

- **التطبيقات المثبّتة هي النظام** — تعمل بصلاحيتك كاملة، لأنها مكوّناته.
- **التطبيق الذي يولّده الذكاء كود طرف ثالث** كتبه نموذج لغوي من وصف بالكلمات.
  يُثبَّت بأدنى صلاحية (الإشعارات فقط)، وكل قدرة أخرى تُطلب منك لحظة الحاجة.

كيف يعمل عمليًا: عند أول ضغطة على زرّ يريد قدرةً غير ممنوحة، **يُرفض النداء
أولًا** ثم يُعرض عليك حوار يحمل النداء الحقيقي بوسائطه — أنت توافق على فعل
معروف لا على وصف مبهم. ثم يُسجَّل ذلك كله في السجل بالتسلسل:

```
008  ✗ fs.write {"path":"/بيتي/من-تطبيق-مولّد.md", …}   «app_x» يحتاج صلاحية files
009  ✓ grant.add {"app":"app_x","cap":"files"}          user
010  ✓ fs.write {"path":"/بيتي/من-تطبيق-مولّد.md", …}   neural
```

قواعد صارمة في النواة: **لا تطبيق يمنح نفسه** (أي `grant.*` من تطبيق يُرفض
بلا حتى عرض طلب)، ونداءا المنح محجوبان عن المُخطِّط أصلًا — وإلا كان الحاجز
عبثًا. اثنتا عشرة قدرة مصنّفة بخطورتها، تُدار من تطبيق «الصلاحيات» وتُسحب
في أي وقت.

## أسطح العمل

ثلاثة أسطح جاهزة وحتى تسعة: كل سطح سياق مستقل بنوافذه. `Ctrl+1..9` للتنقّل،
`Ctrl+Shift+رقم` لنقل النافذة النشطة، و«أغلق الكل» يعني سطحك الحالي لا أسطحًا
لا تراها. النيّة تفهمها أيضًا: «انتقل إلى السطح الثاني»، «سطح جديد».

## ملفات حقيقية

لم يبقَ نظام الملفات افتراضيًا بالكامل:

- **استيراد** بالسحب والإفلات على سطح المكتب أو من منتقي النظام — نصوص وصور
  حتى 3 م.ب للملف، والصور تُعرض داخل «المحتوى».
- **تصدير** ينزّل الملف إلى قرصك بنداء `fs.export`.
- **التخزين** انتقل إلى IndexedDB (حدّ localStorage خمسة ميجابايت تستهلكه صورة
  واحدة)، مع احتياط تلقائي، وإشعار صريح إن تعذّر الحفظ — نظام التشغيل لا يجوز
  أن يفقد جلستك بصمت.

## يُثبَّت ويعمل بلا شبكة

نوفا تطبيق ويب مُثبَّت (PWA): زرّ «ثبّت النظام» يظهر حين يقبل المتصفح ذلك،
فتُشغّلها من قائمة تطبيقات جهازك في نافذة مستقلة بلا شريط متصفح.

عامل الخدمة متحفّظ بقصد: **لا يلمس `/api` إطلاقًا** (طبقة الذكاء تحتاج الحقيقة
اللحظية لا نسخة مخزّنة)، ولا غير GET، ولا نطاقًا آخر. الأصول
الساكنة من الذاكرة، والتنقّل إلى `/os` من الشبكة أولًا والذاكرة عند انقطاعها —
فتُقلع نوفا على طائرة وجلستك محفوظة في IndexedDB.

## تحدّث إليه

نظام تُقاد حالته بالنيّة ينبغي أن يسمعها. زرّ الميكروفون في شريط النيّة
يستمع بالعربية عبر Web Speech API (المتصفح يتعرّف، لا خادمنا، ولا مفتاح)،
وما يُقال يُنفَّذ فورًا. الزر لا يظهر إلا في متصفح يدعمها فعلًا — Chrome و
Edge يدعمانها وفايرفوكس لا — فلا نَعِد بما لا يعمل.

## التطبيقات المثبّتة

| التطبيق | الدور |
| --- | --- |
| **الأوراكل** | حوار واحد يقود النظام؛ يعرض الخطة المنفّذة نداءً نداءً |
| **الحاسبة** | تعبيرات بأولويات صحيحة، وشريط يُحفظ ملفًا |
| **الساعة** | توقيت ومناطق زمنية ومؤقّت يُصدر إشعار نظام مُسجّلًا |
| **الرسّام** | لوحة تُحفظ PNG حقيقيًا في المحتوى ويمكن تصديره |
| **عن النظام** | أرقام النواة والتخزين والجهاز، تُقرأ من الجداول لا مكتوبة يدويًا |
| **المحتوى** | نظام ملفات دلالي: استعلام ووسوم بدل التنقّل في شجرة |
| **الصدفة (nsh)** | أوامر ولغة طبيعية في سطر واحد؛ ما ليس أمرًا يُمرّر كنيّة |
| **الكتابة** | محرّر يكتب عبر نداء نظام، مع خلاصة/توسيع/تحسين بالعصب |
| **المشغل** | تصف تطبيقًا فيولد ويُثبَّت |
| **المراقب** | الوكلاء والنداءات وزمن التخطيط لحظة بلحظة |
| **الخط الزمني** | ارجع بالنظام كله (نوافذ ومظهر وملفات وقواعد) إلى أي نقطة |
| **القواعد** | متى يحدث كذا → افعل كذا، تنفّذها النواة |
| **الصلاحيات** | من يملك ماذا؛ منح وسحب بنداء مُسجّل |
| **الهوية** | المظهر وطبقة الذكاء والتخزين والجلسة |

---

## التشغيل

```bash
npm install
npm run dev     # ثم افتح /os — بلا تسجيل دخول وبلا قاعدة بيانات
```

لتفعيل طبقة العصب أضف `ANTHROPIC_API_KEY` إلى `.env` (ونقاط `/api/nova/*`
محميّة بجلسة التطبيق لأنها تصرف المفتاح).

**بلا مفتاح:** يعمل النظام كاملًا بطبقة الانعكاس — نوافذ، ملفات، صدفة، وكلاء،
أتمتة، إرجاع زمني، وتوليد تطبيقات بمواصفة احتياطية.

**مع `ANTHROPIC_API_KEY`:** يتولّى Claude (`claude-opus-5`) التخطيط الحر
والتوليد الكامل للتطبيقات. المفتاح يُقرأ في وقت التشغيل ولا يعبر إلى المتصفح أبدًا،
ونقاط `/api/nova/*` محميّة بالجلسة نفسها.

**اختصارات:** `Ctrl+K` شريط النيّة · `` Ctrl+` `` تبديل النوافذ ·
`Ctrl+1..9` الأسطح · `Ctrl+Shift+رقم` نقل النافذة · `Ctrl+J` ترتيب شبكة ·
`Ctrl+←` نصفين · `Ctrl+W` إغلاق · `Ctrl+Shift+L` قفل · زر الفأرة الأيمن =
قائمة سياق · سحب نافذة إلى الحافة = التقاط · إفلات ملفات على سطح المكتب = استيراد.

**الهواتف:** تحت 700px تصبح كل نافذة ملء الشاشة والتبديل من الشريط السفلي —
لا تُحجب أي قدرة، يتغيّر شكل العرض فقط.

## الفحوص

```bash
npm test     # 29 فحصًا لثوابت النواة
```

الفحوص ليست تغطية تجميلية: كل دعوى يقوم عليها النظام تُفحَص لأن انكسارها
بصمت يهدم ميزة كاملة:

- **نقاء النواة** — وإلا فالإرجاع الزمني كذب.
- **تماثل قوى إعادة التشغيل** — النداء المتفرّع لا يُحسب مرتين. (هذا الفحص
  وُلد من خلل حقيقي: كانت النافذة التي يفتحها `fs.write` تُسجّل بنفسها وضمن
  أبيها، فيُنتج الرجوع نافذتين حيث كانت واحدة.)
- **رفض الحاجز لما لا يطابق المخطط** — وإلا فصلاحيات الذكاء مفتوحة.
- **رفض مواصفات الواجهات** الخارجة عن اللغة المعلنة.
- **النيّة ذات الفعل تفوز على الاسم** — «أطلق وكيلًا يرتّب الحاسبة وملفاتي»
  يجب أن يطلق وكيلًا لا أن يفتح الحاسبة.
- **التطبيع العربي يحفظ مواقع الحروف** — البحث في نص مُطبَّع والقطع من نص
  أصلي يقطع من الموضع الخطأ ما لم تُحفظ الخريطة.
- **البحث يتجاوز البادئات** — «والنخيل» يجد «النخيل».
- **قاعدة الأتمتة لا تُطلق نفسها** بلا نهاية.
- **حاجز الصلاحيات** — التطبيق المولّد لا يكتب ملفًا قبل الموافقة، ولا يمنح
  نفسه، ولا يرى المُخطِّط نداء المنح، ومكوّنات النظام لا تمرّ بالحاجز.
- **لا ربط بمنتج** — أي نداء أو قدرة تخصّ تطبيقًا بعينه تُعدّ تسرّبًا معماريًا
  ويكسر الفحص.
- **النوايا العامة تحمل معاملاتها** — «احسب 1280 × 12» يفتح الحاسبة محمّلة،
  و«مؤقّت عشر دقائق» يضبط المدّة؛ لا نافذة فارغة ينتظر المستخدم تعبئتها.
- **لا تقشير أعمى للعربية** — «لوحة» ليست «وحة»: تسمية التطبيقات المولّدة
  تقشّر لام الجرّ من صيغ معروفة فقط.

---

## حدود معروفة (بصراحة)

- **الحالة في المتصفح** (IndexedDB مع احتياط localStorage)، لا على الخادم:
  نوفا جلسة على جهازك لا حساب في السحابة. مسح بيانات الموقع = عودة للمصنع.
- **السجل لا يُحفظ بين الجلسات** بقصد؛ الإرجاع الزمني نطاقه الجلسة الحالية.
- **الوكلاء ينتجون عملًا حقيقيًا لا وهميًا:** يقرأون ملفاتك بالبحث الدلالي،
  ويكتبون تقريرًا مبنيًا على ما وجدوه فعلًا (يحلّله Claude إن توفّر المفتاح،
  وإلا يسرده الانعكاس ويصرّح بحدوده). ما يبقى محاكاةً هو **إيقاع** الخطوات
  لا ناتجها، وهم لا يستدعون أدوات خارجية بعد.
- **الإدخال الصوتي غير مُختبَر حيًّا**: بيئة البناء بلا ميكروفون، فالمُتحقَّق
  منه هو كشف القدرة ومسارات الخطأ لا التعرّف نفسه.
- **مسار العصب مُختبَر تجميعيًا ونوعيًا لا حيًّا**: البيئة التي بُني فيها هذا العمل
  بلا `ANTHROPIC_API_KEY`، فكل ما تحقّق عمليًا في المتصفح كان عبر طبقة الانعكاس.
- **ليس نظام تشغيل على معدن**: لا نواة ولا مجدول عمليات ولا مشغّلات أجهزة.
  ما هنا نظام تشغيل بمعنى *الطبقة التي تقود الحالة والواجهة والقدرات* — منفَّذ
  في المتصفح لأن ذلك ما يمكن أن يعمل فعلًا وتراه بعينك اليوم.
