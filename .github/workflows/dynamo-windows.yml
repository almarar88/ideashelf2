name: Build Dynamo for Windows

on:
  workflow_dispatch:
  push:
    tags:
      - "dynamo-v*"

permissions:
  contents: write

jobs:
  windows-installer:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: mbzuh

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: mbzuh/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Build installer
        run: npm run dist:win

      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: Dynamo-Setup-Windows
          path: mbzuh/release/Dynamo-Setup-*.exe
          if-no-files-found: error

      - name: Publish release with installer
        if: startsWith(github.ref, 'refs/tags/dynamo-v')
        uses: softprops/action-gh-release@v2
        with:
          name: الدينامو ${{ github.ref_name }}
          files: mbzuh/release/Dynamo-Setup-*.exe
          body: |
            ملف تثبيت تطبيق «الدينامو» لويندوز (64-بت).

            نزّل `Dynamo-Setup-1.0.0.exe` ثم شغّله واتبع خطوات التثبيت.
            التطبيق يعمل بقاعدة بيانات محلية على جهازك بلا إنترنت.
